#include<stdio.h>
int lcm(int,int);
void main()
{
	int n1,n2;
	printf("Enter The Number");
	scanf("%d%d",&n1,&n2);
	int res=lcm(n1,n2);
	if(res==0)
	printf("LCM =%d\n",res);
}
int lcm(n1,n2)
{
	int res;
	while(n1%n2==0)
		{
			
			return 0;
		}
	else 
	return 1;
}

